{
  "name": "mouse",
  "version": "1.0.0",
  "description": "",
  "main": "mouse.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx esbuild mouse.ts --bundle --outfile=mouse.js --format=iife --platform=browser"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@huggingface/transformers": "^3.8.1",
    "esbuild": "^0.27.3",
    "ruvector": "^0.2.2",
    "typescript": "^5.9.3"
  }
}
